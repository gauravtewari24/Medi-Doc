<!-- <!DOCTYPE html>
<html>
    <head>
        <style>
        table {
            border-collapse: collapse;
        }
        table, th, td {
            border: 1px solid black;
        }
        </style>
    </head>
    <body>
        <div style="width: 85%; margin: 0 7.5%;">
            <div style="text-align: center; padding-left: 50px;">            
                <h3>Paitent Report</h5>
                <!-- <h5><%= student.first_name %></h5>
                <h5><%= student.last_name %></h5>
                <h5><%= student.username %></h5>
                <h5><%= student.phone %>/h5>
                <h5><%= student.gender %></h5>
                <h5><%= student.blood_oxy %></h5>
                <h5><%= student.blood_press %></h5>
                <h5><%= student.sugar %></h5>
                <table>
                <% if (students.user_data.length > 0) { %>
                    <% students.forEach(student => {%>
                       <tr><td><%= student.user_data.pdf_no %></td><td><%= student.date %></td><td><%= student.report %></td><td><%=student.result %></td></tr>
                    <%  });%>
                <% }%>
                </table> -->
             <!-- </div>
        </div>
    </body>
</html> --> -->

<!DOCTYPE html>
<html>
    <head>
        <style>
        table {
            border-collapse: collapse;
        }
        table, th, td {
            border: 1px solid black;
        }
        </style>
    </head>
    <body>
        <div style="width: 85%; margin: 0 7.5%;">
            <div style="text-align: center; padding-left: 50px;">            
                <h3>Student Report</h5>
                <table>
                <% if (students.length > 0) { %>
                    <% students.forEach(student => {%>
                       <tr><td><%= student.name %></td><td><%= student.email %></td><td><%= student.city %></td><td><%=student.country %></td></tr>
                    <%  });%>
                <% }%>
                </table>
             </div>
        </div>
    </body>
</html>